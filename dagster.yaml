run_storage:
  module: dagster_postgres.run_storage
  class: PostgresRunStorage
  config:
    postgres_db:
      username: postgres_user
      password: postgres_password
      hostname: dagster_postgresql
      db_name: postgres_db

event_log_storage:
  module: dagster_postgres.event_log
  class: PostgresEventLogStorage
  config:
    postgres_db:
      username: postgres_user
      password: postgres_password
      hostname: dagster_postgresql
      db_name: postgres_db

schedule_storage:
  module: dagster_postgres.schedule_storage
  class: PostgresScheduleStorage
  config:
    postgres_db:
      username: postgres_user
      password: postgres_password
      hostname: dagster_postgresql
      db_name: postgres_db

scheduler:
  module: dagster.core.scheduler
  class: DagsterDaemonScheduler

# run_launcher:
#   module: dagster_aws.ecs
#   class: EcsRunLauncher
#   config:
#     secrets:
#     - name: aws/secretkey
#       valueFrom: arn:aws:secretsmanager:us-east-1:362955573161:secret:aws/secretkey-bGnJ3I
#     task_definition: arn:aws:ecs:us-east-1:362955573161:task-definition/dagster-task-definition:2
#     container_name: dagster-image
